# boiler plate, filled it automatically
@PACKAGE_INIT@

# include the exported targets, all the important stuff
include("${CMAKE_CURRENT_LIST_DIR}/ColSimTargets.cmake")

# set custom variables to be used in other projects' cmake files
set(ColSim_INCLUDE_DIRS "${CMAKE_CURRENT_LIST_DIR}/../../../include")
set(ColSim_LIBRARIES ColSim::ColSim)
